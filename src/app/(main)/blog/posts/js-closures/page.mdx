export const metadata = {
  title: 'JS-closures',
  frontMatter: {
    postTitle: 'JS closures',
    tags: ['js', 'javascript', 'closures'],
    excerpt: 'A post about JS closures.',
    slug: 'js-closures',
    created: '2025-12-01',
    updated: '2025-12-01',
  }
};





# Js closures

This is a post about JS closures.


# Example

```foo.js

function outerFunction() {
  const outer = 'I am outside!';
  function innerFunction() {
    const inner = 'I am inside!';
    console.log(outer);
    console.log(inner);
  }
  return innerFunction;
}

const innerFunction = outerFunction();

innerFunction();

```
